# calculator
The calculator functions pretty much as expected, but the code needs to be simplified and split into addition functions to aid in legibility and to ease comprehension.

The html structure is a simple collection of div elements which hold the generated buttons from the javascript file, however, although this makes it easier to add new mathematical methods in the future, it means that the js is more complicated than it needs to be.

Fibonacci and factorial buttons have been added to the calculator.